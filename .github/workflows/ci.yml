name: CI

on:
  pull_request:
  push:
    branches: [main, 'release/v*']

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: Rust workspace
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.98.0
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Lint
        run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
      - name: Test
        run: cargo test --locked --workspace --all-targets --all-features
      - name: Check documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --locked --workspace --no-deps

  security-audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: rustsec/audit-check@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  client-ts:
    name: TypeScript client bindings
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.98.0
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - name: Install npm dependencies
        working-directory: crates/signalweave-client-ts
        run: npm ci
      - name: Type-check
        working-directory: crates/signalweave-client-ts
        run: npm run check
      - name: Unit tests (codec + mocked WebTransport client)
        working-directory: crates/signalweave-client-ts
        run: npm test
      - name: Decode checked-in golden fixtures
        working-directory: crates/signalweave-client-ts
        run: |
          npm run test:decode-fixture
          npm run test:decode-tool-call-completed
