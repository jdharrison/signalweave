[package]
name = "signalweave-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Transport-neutral FlatBuffers protocol and bounded framing for Signalweave"
license.workspace = true
repository.workspace = true
publish = false
build = "build.rs"

[lib]
path = "src/lib.rs"

[dependencies]
flatbuffers.workspace = true

[build-dependencies]
flatbuffers-build.workspace = true
flatc-fork.workspace = true

[lints]
workspace = true
